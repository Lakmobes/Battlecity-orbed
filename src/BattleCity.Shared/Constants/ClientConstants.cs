namespace BattleCity.Shared.Constants;

/// <summary>Client-only constants that intentionally differ from the server.</summary>
public static class ClientConstants
{
    /// <summary>Legacy client radar render size (server uses 1800).</summary>
    public const int RadarSize = 2400;
}
