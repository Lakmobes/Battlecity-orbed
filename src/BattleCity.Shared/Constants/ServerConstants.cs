namespace BattleCity.Shared.Constants;

/// <summary>Server-only constants that intentionally differ from the client.</summary>
public static class ServerConstants
{
    /// <summary>Legacy server radar size (client uses 2400).</summary>
    public const int RadarSize = 1800;
}
